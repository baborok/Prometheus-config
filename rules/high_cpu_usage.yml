groups:
  - name: CPU Usage
    rules:
      - alert: HighCPUUsage
        expr: node_cpu_seconds_total{mode="idle"} < 15
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage detected"
